#!/bin/sh

# https://github.com/mth/hirc/blob/master/plans.txt

regex="^\!plan\s"

if [ -n "$CONFIG" ]
then
    echo $regex
    exit
fi

args=$( echo $@ | sed "s/$regex//I" )

echo "1. $args"
echo
echo "2. ???"
echo
echo "3. $( shuf -n1 plan.txt )"
